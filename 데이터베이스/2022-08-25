=========================
③ select
① from 
② where
=========================

[다중 비교 조건]
- between A and B : A와 B사이 값(A와 B값도 포함)
- in(A,B) : 값 목록 중의 값과 일치 (A또는 B의 값인 여러개의 값을 찾음)
- like : 문자 패턴 일치 (전체가 아닌 데이터의 일부만 알 때 검색할 수 있음)
  => % : 문자가 오지 않거나 여러개 올 수 있음
  => _ : 문자가 하나만 올 수 있음
- is null : 널 값 (null값 찾을 때)
