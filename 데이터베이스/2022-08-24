- 테이블에서 데이터는 행단위의 데이터로 관리가 이루어진다.
- select list절에 명시한 컬럼의 순서가 그대로 출력이 이루어진다.

[parse 파싱]
: oracle 서버 내에서 컴파일하는 것 
- 1단계 : 검증 
- 2단계 : 실행 계획 수립 
- 3단계 : 실행 

[산술 연산]
- col+상수
- co+ol
=> 숫자 가능
=> 문자 불가능
=> 날짜 일수만 +, - 가능

[nvl]
nvl(컬럼이름, n)
=> 컬럼 이름을 n(숫자)으로 바꿔서 계산해라

[별 열칭 (alias)]
- 1개의 컬럼 당 1개의 alias명을 줄 수 있다.
- alias 주는 방법
  1. as     ex) as name  => 결과는 NAME
  2. 　(공백) ex)    name  => 결과는 NAME
  3. ""     ex) "name"   => 결과는 name
    1,2 는 단수 문자로 대문자로 출력이 되고
    3 은 복수 문자로 큰따옴표 안에 적힌 그대로 출력이 된다.
